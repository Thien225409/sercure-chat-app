# --- Dependencies ---
node_modules/
package-lock.json # Tùy chọn: Nếu bạn muốn đồng bộ phiên bản chính xác thì KHÔNG ignore file này. Nhưng nếu là bài tập cá nhân thì có thể ignore cho nhẹ. (Khuyên dùng: KHÔNG ignore package-lock.json nếu làm team)

# --- Environment Variables (QUAN TRỌNG NHẤT) ---
# Chặn tất cả các file chứa biến môi trường và khóa bí mật
.env
.env.local
.env.*.local
*.pem
*.key

# --- Build Outputs (Client - Vite/React) ---
dist/
dist-ssr/
build/
*.local

# --- Logs ---
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs
*.log
npm-debug.log

# --- IDE / Editor System Files ---
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# --- Testing / Coverage ---
coverage/